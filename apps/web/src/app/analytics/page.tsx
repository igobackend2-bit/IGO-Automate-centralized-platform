import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AnalyticsPage() {
  return (
    <PlaceholderPage
      title="Delivery & Reply Analytics"
      phase="Phase 2"
      description="Sent/delivered/read/replied per campaign and per sub-brand, sourced from the messages table (fed by Evolution API, listmonk, and WATI webhooks)."
    />
  );
}
