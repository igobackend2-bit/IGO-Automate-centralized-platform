import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AriaPage() {
  return (
    <PlaceholderPage
      title="Aria Persona Console"
      phase="Phase 2–4"
      description="Edit the prompt/RAG facts feeding the current Groq/Gemini persona (see apps/api/src/personas/aria.js). Later, a toggle to A/B and route to the fine-tuned Ollama model once Phase 4 is ready."
    />
  );
}
