import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ContactsPage() {
  return (
    <PlaceholderPage
      title="Unified Contacts"
      phase="Phase 2"
      description="Import/dedupe customers across WATI's old lead sheets, the Zoho Campaigns trial list, and Supabase; tag by sub-brand. The backend already exposes a read-only /api/contacts endpoint against the customers table."
    />
  );
}
